{% extends 'base.html.twig' %}

{% block title %}Cartera{% endblock %}

{% block body %}
    <h1>Cartera</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ cartera.id }}</td>
            </tr>
            <tr>
                <th>Nombre</th>
                <td>{{ cartera.nombre }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('cartera_index') }}">back to list</a>

    <a href="{{ path('cartera_edit', {'id': cartera.id}) }}">edit</a>

    {{ include('cartera/_delete_form.html.twig') }}
{% endblock %}
