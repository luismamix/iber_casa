{% extends 'base.html.twig' %}

{% block title %}Status2{% endblock %}

{% block body %}
    <h1>Status2</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ status2.id }}</td>
            </tr>
            <tr>
                <th>Descripcion</th>
                <td>{{ status2.descripcion }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('status2_index') }}">back to list</a>

    <a href="{{ path('status2_edit', {'id': status2.id}) }}">edit</a>

    {{ include('status2/_delete_form.html.twig') }}
{% endblock %}
