<form method="post" action="{{ path('tipo_usuario_delete', {'id': tipo_usuario.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ tipo_usuario.id) }}">
    <button class="btn btn-danger">Borrar</button>
</form>
