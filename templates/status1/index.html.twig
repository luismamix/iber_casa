{% extends 'base.html.twig' %}

{% block title %}Status1 index{% endblock %}

{% block body %}
    <h1>Status1 index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>Descripcion</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for status1 in status1s %}
            <tr>
                <td>{{ status1.id }}</td>
                <td>{{ status1.descripcion }}</td>
                <td>
                    <a href="{{ path('status1_show', {'id': status1.id}) }}">show</a>
                    <a href="{{ path('status1_edit', {'id': status1.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="3">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('status1_new') }}">Create new</a>
{% endblock %}
