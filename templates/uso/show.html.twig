{% extends 'base.html.twig' %}

{% block title %}Uso{% endblock %}

{% block body %}
    <h1>Uso</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ uso.id }}</td>
            </tr>
            <tr>
                <th>Descripcion</th>
                <td>{{ uso.descripcion }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('uso_index') }}">back to list</a>

    <a href="{{ path('uso_edit', {'id': uso.id}) }}">edit</a>

    {{ include('uso/_delete_form.html.twig') }}
{% endblock %}
