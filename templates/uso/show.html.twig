{% extends 'base.html.twig' %}

{% block title %}Uso{% endblock %}

{% block body %}
    <h1>Uso
    <span><i style="font-size:38px;" class="fas fa-eye text-dark ml-1"></i></span></h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ uso.id }}</td>
            </tr>
            <tr>
                <th>Descripcion</th>
                <td>{{ uso.descripcion }}</td>
            </tr>
        </tbody>
    </table>

    <a class="btn btn-primary" role="button" href="{{ path('uso_index') }}">Volver al listado</a>

    <a class="btn btn-secondary" role="button" href="{{ path('uso_edit', {'id': uso.id}) }}">Modificar</a>

    {{ include('uso/_delete_form.html.twig') }}
{% endblock %}
