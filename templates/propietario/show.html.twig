{% extends 'base.html.twig' %}

{% block title %}Propietario{% endblock %}

{% block body %}
    <h1>Propietario</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ propietario.id }}</td>
            </tr>
            <tr>
                <th>Nombre</th>
                <td>{{ propietario.nombre }}</td>
            </tr>
        </tbody>
    </table>

    <a class="btn btn-primary" role="button" href="{{ path('propietario_index') }}">Volver al listado</a>

    <a class="btn btn-secondary" role="button" href="{{ path('propietario_edit', {'id': propietario.id}) }}">Modificar</a>

    {{ include('propietario/_delete_form.html.twig') }}
{% endblock %}
