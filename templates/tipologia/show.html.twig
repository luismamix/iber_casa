{% extends 'base.html.twig' %}

{% block title %}Tipologia{% endblock %}

{% block body %}
    <h1>Tipologia</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ tipologium.id }}</td>
            </tr>
            <tr>
                <th>Descripcion</th>
                <td>{{ tipologium.descripcion }}</td>
            </tr>
        </tbody>
    </table>

    <a class="btn btn-primary" role="button" href="{{ path('tipologia_index') }}">Volver al listado</a>

    <a class="btn btn-secondary" role="button" href="{{ path('tipologia_edit', {'id': tipologium.id}) }}">Modificar</a>

    {{ include('tipologia/_delete_form.html.twig') }}
{% endblock %}
